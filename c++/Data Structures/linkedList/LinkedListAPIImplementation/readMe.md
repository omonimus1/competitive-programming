# LinkedList API implementation

This program shows how to:
* Print a Linked-List
* Count the number of elements in the LinkedList, knowing so it's size
* Push (or add) a new element inside the LinkedList

Next updates coming soon 
